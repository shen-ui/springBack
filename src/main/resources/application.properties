# Hosts application locally on port 3000 instead of a busier port
server.port=3000

## spring JPA to connect to a database
spring.datasource.url=jdbc:postgresql://vicblogrds.cviy0ljarurq.us-east-2.rds.amazonaws.com:5432/blogDB
spring.datasource.username=postgres
spring.datasource.password=2022TRoadster!
## creates a schema and sets it as the schema to deploy Springboots table to.
spring.jpa.properties.hibernate.default_schema=blogSchema
## create_namespaces allows springJPA to create schema if not exists.
spring.jpa.properties.hibernate.hbm2ddl.create_namespaces=true
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true